# Application features

## Persist Component Props

Components like `Table` have multiple properties that users may be able to change at runtime, for example

- Hiding and showing Columns
- Changing the order od columns within the table
- Sorting by one or more columns
