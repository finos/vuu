<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.finos.vuu</groupId>
        <artifactId>example</artifactId>
        <version>11.3.0-SNAPSHOT</version>
    </parent>

    <packaging>pom</packaging>
    <artifactId>python-integration</artifactId>
    <name>${project.artifactId}</name>
    <description>
        Starts the Vuu view server from Python, with the JVM embedded in the Python process via
        JPype, mirroring the startup sequence in example/main-java's VuuExampleMain. This module
        has no Java/Scala sources of its own - it resolves the runtime classpath that the Python
        driver script (python/start_server.py) needs to call directly into org.finos.vuu.core, and
        even the custom "Snakes" table's module and provider are defined in Python (via JPype
        JImplements proxies for org.finos.vuu.provider.Provider and scala.Function2) rather than
        compiled Java.
    </description>

    <dependencies>

        <dependency>
            <groupId>org.finos.vuu</groupId>
            <artifactId>vuu</artifactId>
            <version>11.3.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>org.finos.vuu</groupId>
            <artifactId>vuu-java</artifactId>
            <version>11.3.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>org.finos.vuu</groupId>
            <artifactId>price</artifactId>
            <version>11.3.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>org.finos.vuu</groupId>
            <artifactId>order</artifactId>
            <version>11.3.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>org.finos.vuu</groupId>
            <artifactId>permission</artifactId>
            <version>11.3.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${vuu.logback.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>write-classpath-for-python</id>
                        <!-- Before "test" (not "package", where this originally lived) so the
                             python-tests profile's pytest run below - bound to the "test" phase -
                             has target/classpath.txt available. Resolving dependencies doesn't
                             need compilation, so running this earlier than "package" doesn't
                             change anything for a plain `mvn package` build. -->
                        <phase>process-test-resources</phase>
                        <goals>
                            <goal>build-classpath</goal>
                        </goals>
                        <configuration>
                            <outputFile>${project.build.directory}/classpath.txt</outputFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <profile>
            <!-- Opt-in: `mvn -pl example/python-integration -am test -Ppython-tests`.
                 Not part of the default "test" phase - it needs network access to pip install
                 jpype1/pytest that a plain `mvn test`/`verify` (this module is in the root
                 reactor, so that includes CI) shouldn't require. -->
            <id>python-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>run-python-smoke-test</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <executable>bash</executable>
                            <workingDirectory>${project.basedir}/python</workingDirectory>
                            <arguments>
                                <argument>run_tests.sh</argument>
                            </arguments>
                            <environmentVariables>
                                <!-- jpype.startJVM()'s JVM-finder falls back to platform-specific
                                     lookups (e.g. macOS's /usr/libexec/java_home) that can pick a
                                     different JVM to the one Maven itself is running under, or
                                     fail outright if none is registered with the OS. ${java.home}
                                     is Maven's own built-in property for the JRE/JDK home it's
                                     running on (== JAVA_HOME on JDK 9+, which this repo requires),
                                     so this pins the child process to the same JVM. -->
                                <JAVA_HOME>${java.home}</JAVA_HOME>
                            </environmentVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
